1. Looking at the backtrace output, which function called syscall?
usertrap

2. What is the value of p->trapframe->a7 and what does that value represent?
0x7

3. What was the previous mode that the CPU was in?
user mode

4. Write down the assembly instruction the kernel is panicing at. 
lw s2, 0(zero)

Which register corresponds to the variable num?
num reg: s2

5. Why does the kernel crash? Hint: look at figure 3-3 in the text; 
read value in address 0.

is address 0 mapped in the kernel address space?  no.
Is that confirmed by the value in scause above? load page fault.


6. What is the name of the binary that was running when the kernel paniced? What is its process id (pid)?
"initcode"